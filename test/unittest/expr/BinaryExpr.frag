#version 460

void main() {
    int x;
    int y;

    x + y;
    x - y;
    x * y;
    x / y;
    x % y;
    x & y;
    x | y;
    x ^ y;
    x << y;
    x >> y;
    x && y;
    x || y;
    x < y;
    x > y;
    x <= y;
    x >= y;
    x == y;
    x != y;
}